{
  "compilerOptions": {
    "target": "es2016",                      // Compila JavaScript usando le funzionalità di ES2016 come target.
    "module": "commonjs",                    // Usa il sistema di moduli CommonJS, ideale per Node.js.
    "esModuleInterop": true,                 // Abilita l'interoperabilità tra moduli CommonJS e ESModule.
    "forceConsistentCasingInFileNames": true,// Impone un uso coerente delle maiuscole nei nomi dei file.
    "strict": true,                          // Attiva tutte le opzioni di controllo rigoroso.
    "skipLibCheck": true,                    // Salta il controllo dei tipi per i file delle librerie.
    "outDir": "./dist"                       // Imposta "dist" come cartella di output per i file compilati
  },
  "include": [
    "src/**/*"                               // Specifica i file da compilare all'interno della cartella "src"
  ],
  "exclude": [
    "node_modules",                          // Esclude i moduli npm dalla compilazione
    "dist"                                   // Evita la ricompilazione dei file già compilati
  ]
}
